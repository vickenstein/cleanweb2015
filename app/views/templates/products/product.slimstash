- for products.each do |product|
  .product
    .name {{product.name}}
    .price {{product.total_cost}}
    .qty {{product.qty}}
    .a(href="{{product.product_url}}")
      img(src="{{product.image_url}}")
    .lifetime_savings.attribute {{product.lifetime_savings}}
    .co2_savings.attribute {{product.co2_savings}}
    .energy_cost.attribute {{product.energy_cost}}